<Update date="2026-04-30" title="Initialize Prompty tracing and Azure OpenAI integration for Contoso Chat">
- Load environment variables at startup with dotenv and initialize Prompty tracing (console and JSON) across the Contoso Chat modules.
- Register Prompty tracers via Tracer for consistent distributed tracing.
- Introduce a complete chat flow that retrieves customer data, finds relevant products using embeddings, and queries Azure OpenAI via Prompty for responses.
- Implement embedding generation and semantic product retrieval using Azure OpenAI and Azure Cognitive Search, wired through the product.find_products workflow.
- Prepare model configuration from environment variables (Azure OpenAI endpoint and API version) and integrate tracing into core paths (chat_request, product search, and evaluators).
</Update>